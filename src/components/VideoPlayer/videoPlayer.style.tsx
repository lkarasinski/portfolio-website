import styled from 'styled-components';

export const StyledWrapper = styled.div`
    aspect-ratio: 16/9;
    max-width: 1024px;
    margin: 25px auto;

    video {
        border-radius: 16px;
    }
`;
